# COVID-19-Analysis

Scraper.py extracts the data from a JSON file available online and stores it to a database.

Graph.py asks the user to enter the unique district id and shows the matplotlib graph.
